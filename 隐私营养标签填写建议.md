# Mind U隐私营养标签填写建议

更新日期：2026 年 8 月 5 日

本建议以当前 Release 二进制行为为准：无账号、无广告、无分析 SDK、无开发者自建后端、无自动 CloudKit 同步；记录、提醒与搜索历史保存在本机，定位仅用于向 Apple WeatherKit 请求日历天气，备份只在用户主动操作后导出。

> 最终答案必须在上传的 Release 构建和 App Store Connect 中逐项复核。若发版前重新启用 CloudKit，或改变 WeatherKit、语音识别、SDK 或服务端行为，应重新填写。

## 1. 建议总答案

当前版本可按 `Data Not Collected` 方向核对，依据是：

- Release 不启用 CloudKit；记录、提醒、搜索历史不离开本机，除非用户主动导出备份
- WeatherKit 和系统语音识别由 Apple 提供；应用不把定位或录音发送到开发者控制的服务
- 应用没有广告、分析、归因、客服或崩溃上报 SDK

Apple 对“收集”的定义是：数据离开设备后，开发者或第三方合作方可以访问并保留超过完成实时请求所需的时间。Apple 自身通过系统框架或服务处理的数据，不等于开发者收集的数据。

## 2. App Store Connect 逐项建议

### Data Used to Track You

- 选择 `No`

### Data Linked to You

- 当前可按 `No data collected` 方向核对

### Data Not Linked to You

- 当前可按 `No data collected` 方向核对

### 需要在问卷中如实确认的能力

- Release 不使用 CloudKit 自动同步
- 使用 Apple WeatherKit，并仅在用户主动请求天气时使用“使用 App 时”定位
- 使用 Apple Speech 框架进行中文 `zh-CN` 与英文 `en-US` 语音识别
- 不使用 App Tracking Transparency，因为没有跟踪行为

不要把用户主动保存到 iCloud Drive 的备份文件误写成 App 自动云同步，也不要把 Apple 自己处理的数据误写成开发者收集。若 App Store Connect 的具体问题要求披露 Apple 服务用途，应按实际能力选择，而不是机械选择空白。

## 3. 数据类别判断

在当前行为保持不变时，以下数据不应按“开发者收集”勾选：

- `Other User Content`：记录正文保存在本机；用户主动导出的备份由用户控制，开发者不可见
- `Search History`：仅保存在本机，开发者不可见
- `Precise Location` / `Coarse Location`：仅用于 Apple WeatherKit 请求，不发送给开发者
- `Audio Data`：应用不保存录音，也不发送到开发者服务；系统语音识别可能由 Apple 处理
- `Identifiers`、`Usage Data`、`Diagnostics`：无开发者侧分析或上报

如果未来让开发者、客服、分析平台或其他第三方能访问以上任何数据，必须立即改为披露相应类别与用途。

## 4. Privacy Manifest

工程内 `YijiApp/Resources/PrivacyInfo.xcprivacy` 应随 App 一起打包，当前声明：

- `NSPrivacyAccessedAPICategoryUserDefaults`，理由 `CA92.1`：读写仅当前 App 可访问的偏好数据
- `NSPrivacyAccessedAPICategoryFileTimestamp`，理由 `C617.1`：读取 App 容器内备份存储文件的时间戳
- `NSPrivacyTracking = false`
- 无开发者收集的数据类型

每次归档后都要确认该文件实际出现在 `YijiApp.app/PrivacyInfo.xcprivacy`，不能只确认源文件存在。

## 5. 提交前最后自检

1. 最终 Release entitlement 是否不包含 CloudKit，且 `YijiCloudSyncEnabled = NO`。
2. 定位是否只用于 WeatherKit，且不写入记录、备份或 Release 日志。
3. 语音录音是否不保存、不发送到开发者服务。
4. 工程是否仍无第三方分析、广告、客服、崩溃或归因 SDK。
5. `PrivacyInfo.xcprivacy` 是否包含在最终归档。
6. 隐私政策、App 内隐私政策入口和 App Store Connect 答案是否一致。

## 6. 触发重填的变化

- 加入邮箱注册、登录或账号体系
- 让开发者或客服访问用户内容
- 接入开发者自建后端、腾讯云或其他服务器 API
- 接入崩溃分析、统计、广告、归因或客服 SDK
- 保存或上传定位、录音、搜索记录或设备标识
- 加入家庭 group、共享或协作
- 接入支付并保存交易相关资料

## 7. 配套文档

- [隐私政策草案.md](/Users/Kenneth/Documents/Code/EJ/隐私政策草案.md:1)
- [支持页面草案.md](/Users/Kenneth/Documents/Code/EJ/支持页面草案.md:1)
- [App Store Connect 上架资料草案.md](/Users/Kenneth/Documents/Code/EJ/App%20Store%20Connect%20%E4%B8%8A%E6%9E%B6%E8%B5%84%E6%96%99%E8%8D%89%E6%A1%88.md:1)
