# 腾讯云静态合规页面部署说明

更新时间：2026 年 6 月 9 日

本文档用于把易记的公开合规页面部署到你现有腾讯云轻量服务器上，同时与已经在线运行的 Web 应用保持隔离。

## 目标

将以下静态页面公开访问：

- `site/index.html`
- `site/privacy.html`
- `site/support.html`

建议最终访问地址：

- `https://legal.your-domain.com/privacy.html`
- `https://legal.your-domain.com/support.html`

## 为什么不会影响现有 Web 应用

推荐采用这套隔离方案：

- 单独子域名，例如 `legal.your-domain.com`
- 单独静态目录，例如 `/var/www/yiji-legal`
- 单独 Nginx `server` 配置
- 不接数据库
- 不接现有后端
- 不复用现有前端路由
- 只提供静态 HTML 和 CSS 文件

这样做的结果是：

- 现有 Web 应用继续跑它自己的站点和接口
- 合规页面只占用同一台服务器上的另一个 `server_name`
- 双方互不共享代码入口和运行时

## 建议目录结构

服务器上建议这样放：

```text
/var/www/yiji-legal/
  index.html
  privacy.html
  support.html
  assets/
    styles.css
```

## 建议部署步骤

### 1. 准备子域名

在你的域名 DNS 控制台新增一条记录：

- 主机记录：`legal`
- 记录类型：`A`
- 记录值：你的腾讯云轻量服务器公网 IP

这样最终域名就是：

`legal.your-domain.com`

### 2. 上传静态文件

将仓库里的 `site/` 目录内容上传到服务器目标目录：

```bash
mkdir -p /var/www/yiji-legal
```

上传完成后，`/var/www/yiji-legal` 下应直接包含：

- `index.html`
- `privacy.html`
- `support.html`
- `assets/styles.css`

## Nginx 配置示例

下面是一个独立站点配置示例。它只服务静态文件，不代理任何应用。

```nginx
server {
    listen 80;
    server_name legal.your-domain.com;

    root /var/www/yiji-legal;
    index index.html;

    access_log /var/log/nginx/yiji-legal-access.log;
    error_log /var/log/nginx/yiji-legal-error.log;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

如果你已经有 HTTPS，建议再补一个 `443` 的独立配置块。  
重点是：

- `server_name` 和你现有 Web 应用不同
- `root` 指向新的静态目录
- 不写 `proxy_pass`

## HTTPS 建议

Apple 提交资料时，隐私政策和支持页建议直接使用 HTTPS。

推荐做法：

- 给 `legal.your-domain.com` 单独申请 SSL 证书
- 用 Nginx 为该子域名单独绑定证书

如果你主站已经在同一台 Nginx 上跑 HTTPS，这一步通常只是再加一个新的 `server_name`，不会影响主站。

## 可直接填写到 App Store Connect 的字段

部署完成后，可以这样填写：

- `Privacy Policy URL`：`https://legal.your-domain.com/privacy.html`
- `Support URL`：`https://legal.your-domain.com/support.html`

`Marketing URL` 可以暂时留空，或者后面补充。

## 上线前必须替换的占位内容

当前静态页面里仍保留了这些占位：

- 支持邮箱
- 正式域名示例

提交 App Store 前，请至少替换：

1. `待替换为真实邮箱`
2. `replace before release`
3. `your-domain.com`

## Apple 合规角度的实际含义

这套页面：

- 不是 Web App
- 不提供登录
- 不提供支付
- 不提供用户生成内容运营
- 不会把 App 变成“网页壳”

它只是公开可访问的说明页面，用来承接：

- 隐私政策链接
- 支持链接
- 用户查看基础说明的入口

这与 Apple 对原生 App 上架的常规要求并不冲突。

## 推荐你的实际执行顺序

1. 先把 `site/` 上传到服务器测试目录
2. 配好 `legal` 子域名和 Nginx 独立站点
3. 浏览器打开 `privacy.html` 和 `support.html`，确认公网可访问
4. 把占位邮箱替换成真实邮箱
5. 再把正式 URL 填到 App Store Connect

## 当前仓库内相关文件

- [site/index.html](/Users/Kenneth/Documents/Code/EJ/site/index.html:1)
- [site/privacy.html](/Users/Kenneth/Documents/Code/EJ/site/privacy.html:1)
- [site/support.html](/Users/Kenneth/Documents/Code/EJ/site/support.html:1)
- [site/assets/styles.css](/Users/Kenneth/Documents/Code/EJ/site/assets/styles.css:1)
