# Mind U iOS 真机试用说明

## 目标

这份说明用于让测试者在不阅读代码的前提下，完成一轮基础真机试用，并快速发现 MVP 当前的关键问题。

## 环境要求

- macOS 已安装 Xcode
- 已能打开 `Yiji.xcodeproj`
- 有一台可运行 iOS 17 及以上的真机，或至少一台 iOS 模拟器

## 启动方式

1. 打开 `Yiji.xcodeproj`
2. 选择 `YijiApp` scheme
3. 选择真机或模拟器
4. 点击运行

## 建议试用顺序

### 1. 首次权限链路

1. 进入“录入”页
2. 点击开始听写
3. 分别观察语音识别、麦克风权限请求是否正常出现
4. 如果拒绝权限，确认页面会显示错误提示，并出现“前往系统设置开启语音权限”

预期：

- 已授权时可以正常开始录音
- 拒绝权限时不会崩溃
- 用户能明确知道下一步去哪里开启权限

### 2. 文字记录链路

1. 在“录入”页输入一句位置记录，例如“我把户口本放在红抽屉最上面”
2. 点击保存

预期：

- 记录能保存成功
- 应用自动切回首页
- 首页自动滚动到新记录附近
- 新记录会短暂显示“刚保存”标识

### 2A. 英文界面与英文语音链路

1. 打开 Mind U“设置”页，在“语言”中选择 English
2. 确认首页、底部导航、设置、日历、提醒和权限说明都使用英文
3. 在真机上按住麦克风说一句英文，例如 “The spare key is in the desk drawer”
4. 确认识别结果为英文，并可保存为普通笔记
5. 搜索 `spare key`，确认可以找回该记录
6. 不退出 App，直接切回简体中文，确认界面和日期格式立即恢复中文

预期：

- English 界面使用 `en-US` 语音转写，简体中文界面使用 `zh-CN`
- 选择会被保存；重新打开 App 后仍保持上次选择，“跟随系统”则恢复使用 iPhone/单 App 系统语言
- 英文文字和英文语音结果可以保存与关键词搜索
- 当前版本不把英文句子自动解析为提醒或物品位置；这项语义解析仍以中文为主
- 切换语言不改变或删除已经保存的数据

### 3. 提醒记录链路

1. 在“录入”页输入一句提醒，例如“明天下午三点提醒我交物业费”
2. 点击保存
3. 进入“设置”页，确认通知权限状态
4. 将提醒时间设为 2～3 分钟后，锁屏等待系统通知

预期：

- 提醒记录能保存
- 提醒会出现在提醒管理页
- 已授权通知时，真实提醒通知可正常送达

### 4. 搜索链路

1. 进入“搜索”页
2. 搜索“户口本”或刚才录入的关键词
3. 再次关闭并重新打开 App

预期：

- 搜索结果能命中刚保存的记录
- 最近搜索历史会保留

### 5. 连续录入稳定性

1. 连续补充至少 10 条位置、提醒和普通笔记记录
2. 关闭并重新打开 App

预期：

- 所有记录仍可读取和搜索
- 记录顺序、分类和提醒关系保持一致
- 不应出现付费页、订阅页或升级页

### 6. 备份导出与恢复

1. 在“设置”页点击“生成备份文件”
2. 分享或保存导出的 JSON 文件
3. 点击“导入备份文件”
4. 重新选择刚才导出的文件

预期：

- 导出成功
- 导入成功
- 记录、提醒和最近搜索历史都能恢复
- 导入后自动回到首页，并高亮最新导入的一条记录

### 7. 日历天气与定位降级

1. 首次进入“日历”页，拒绝定位权限
2. 确认日历、记录、搜索、提醒和备份仍可使用
3. 到系统设置重新允许“使用 App 时”定位
4. 返回日历并点击“获取天气”或“刷新”

预期：

- 拒绝定位时只影响天气，不阻断核心功能
- 授权后能显示未来天气，或给出可理解的暂时不可用提示
- App 不应显示开发调试信息或经纬度

### 8. Release 本机存储边界

1. 使用 Release Archive 或 TestFlight 安装包启动 App
2. 打开“设置”，确认“备份与恢复”中不出现自动 iCloud 同步状态
3. 断开网络后新增、修改和删除记录，再重启 App
4. 确认本机数据仍然完整，并能正常导出 JSON 备份

预期：

- Release 使用本机存储，不依赖 CloudKit 或开发者服务器
- 无网络时记录、搜索、提醒和备份仍可使用；天气与在线语音能力可按系统条件降级
- App 不宣传或暗示两台设备会自动同步

### 9. iCloud Drive 手动恢复

1. 在第一台设备导出备份文件
2. 通过系统分享面板将文件保存到“文件”App 或 iCloud Drive
3. 在第二台设备打开 Mind U，进入“设置”页
4. 点击“导入备份文件”
5. 从“文件”或 iCloud Drive 选择刚才保存的 JSON 文件

预期：

- 第二台设备可正常看到该备份文件
- 导入成功
- 记录、提醒和最近搜索历史都能恢复
- 手动备份恢复仍可作为卸载前保护方案

### 10. 错误文件导入

1. 尝试导入空文件
2. 尝试导入非 JSON 文件，或随便构造一个无关 JSON 文件

预期：

- 应用不会崩溃
- 会提示备份文件为空、不可读取或格式不正确
- 提示应能让非技术人员理解

### 11. 删除与详情一致性

1. 从首页或搜索页进入一条记录详情
2. 删除该记录
3. 返回列表并再次查看首页

预期：

- 删除成功后详情页不会残留旧数据
- 首页列表不应保留已删除记录
- 如果删除的是当前高亮记录，高亮状态应同步消失

## 重点观察项

- 页面提示是否清楚，是否会在不相关页面残留太久
- 权限被拒后是否还有下一步指引
- 保存、删除、导入后，用户是否总能明确看到结果
- 导入错误文件时，文案是否可理解
- 用户是否能理解跨设备转移需要手动导出和导入备份

## 当前已知产品约束

- 只支持无账号、本机存储的首发 MVP；跨设备转移使用手动 JSON 备份
- 首发版本不做注册、登录、group 共享或任务传递
- 不开发付费功能
- CloudKit 实验能力仅保留在 Debug；未来重新启用前必须完成离线编辑、并发新增/修改/删除、导入冲突和 Production schema 验证
