# 易记 iOS 真机试用说明

## 目标

这份说明用于让测试者在不阅读代码的前提下，完成一轮基础真机试用，并快速发现 MVP 当前的关键问题。

## 环境要求

- macOS 已安装 Xcode
- 已能打开 `Yiji.xcodeproj`
- 有一台可运行 iOS 17 及以上的真机，或至少一台 iOS 模拟器

## 启动方式

1. 打开 `Yiji.xcodeproj`
2. 选择 `YijiApp` scheme
3. 选择真机或模拟器
4. 点击运行

## 建议试用顺序

### 1. 首次权限链路

1. 进入“录入”页
2. 点击开始听写
3. 分别观察语音识别、麦克风权限请求是否正常出现
4. 如果拒绝权限，确认页面会显示错误提示，并出现“前往系统设置开启语音权限”

预期：

- 已授权时可以正常开始录音
- 拒绝权限时不会崩溃
- 用户能明确知道下一步去哪里开启权限

### 2. 文字记录链路

1. 在“录入”页输入一句位置记录，例如“我把户口本放在红抽屉最上面”
2. 点击保存

预期：

- 记录能保存成功
- 应用自动切回首页
- 首页自动滚动到新记录附近
- 新记录会短暂显示“刚保存”标识

### 3. 提醒记录链路

1. 在“录入”页输入一句提醒，例如“明天下午三点提醒我交物业费”
2. 点击保存
3. 进入“我的”页，确认通知权限状态
4. 如已授权，发送 10 秒测试通知

预期：

- 提醒记录能保存
- 提醒会出现在提醒管理页
- 已授权通知时，测试通知可正常送达

### 4. 搜索链路

1. 进入“搜索”页
2. 搜索“户口本”或刚才录入的关键词
3. 再次关闭并重新打开 App

预期：

- 搜索结果能命中刚保存的记录
- 最近搜索历史会保留

### 5. 30 条限制

1. 连续补充记录直到达到 `30` 条
2. 尝试继续新增第 `31` 条

预期：

- 第 `31` 条会被拦截
- 页面提示“当前版本最多可记录 30 条。”
- 不应出现付费页、订阅页或升级页

### 6. 备份导出与恢复

1. 在“我的”页点击“生成备份文件”
2. 分享或保存导出的 JSON 文件
3. 点击“导入备份文件”
4. 重新选择刚才导出的文件

预期：

- 导出成功
- 导入成功
- 记录、提醒和最近搜索历史都能恢复
- 导入后自动回到首页，并高亮最新导入的一条记录

### 7. iCloud 自动同步

如果手边有第二台使用同一 Apple 账号并开启 iCloud 的 iPhone 或 iPad，再补一轮：

1. 在第一台设备保存一条新记录
2. 保持第一台设备网络正常
3. 在第二台设备打开易记并等待同步
4. 观察新记录是否自动出现
5. 如有提醒，再确认提醒内容也同步到第二台设备

预期：

- 第二台设备可自动看到新记录
- 不需要应用内账号登录
- 设置页应显示 iCloud 已连接或可理解的不可用状态
- 自动同步失败时，用户仍可退回到手动备份恢复路径

### 8. iCloud Drive 手动恢复

1. 在第一台设备导出备份文件
2. 通过系统分享面板将文件保存到“文件”App 或 iCloud Drive
3. 在第二台设备打开易记，进入“我的”页
4. 点击“导入备份文件”
5. 从“文件”或 iCloud Drive 选择刚才保存的 JSON 文件

预期：

- 第二台设备可正常看到该备份文件
- 导入成功
- 记录、提醒和最近搜索历史都能恢复
- 手动备份恢复仍可作为卸载前保护方案

### 9. 错误文件导入

1. 尝试导入空文件
2. 尝试导入非 JSON 文件，或随便构造一个无关 JSON 文件

预期：

- 应用不会崩溃
- 会提示备份文件为空、不可读取或格式不正确
- 提示应能让非技术人员理解

### 10. 删除与详情一致性

1. 从首页或搜索页进入一条记录详情
2. 删除该记录
3. 返回列表并再次查看首页

预期：

- 删除成功后详情页不会残留旧数据
- 首页列表不应保留已删除记录
- 如果删除的是当前高亮记录，高亮状态应同步消失

## 重点观察项

- 页面提示是否清楚，是否会在不相关页面残留太久
- 权限被拒后是否还有下一步指引
- 保存、删除、导入后，用户是否总能明确看到结果
- 导入错误文件时，文案是否可理解
- 自动同步不可用时，用户是否仍能理解手动备份恢复方案

## 当前已知产品约束

- 只支持无账号的本地优先 MVP，CloudKit 同步依赖用户自己的 iCloud
- 首发版本不做注册、登录、group 共享或任务传递
- 记录数限制为 `30`
- 不开发付费功能
- Launch Screen 最终视觉待设计师 Logo 确认后再补
